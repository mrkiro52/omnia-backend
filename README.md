# Kiro.Team.Edu Backend API

Backend API для образовательной платформы kiro.team.edu

## 🚀 Быстрый старт

### Установка зависимостей
```bash
cd backend
npm install
```

### Настройка окружения
```bash
cp .env.example .env
# Отредактируйте .env файл под ваши настройки
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Запуск в продакшене
```bash
npm start
```

## 📚 API Документация

### Базовый URL
```
http://localhost:5000/api
```

### Endpoints

#### Authentication
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Авторизация пользователя

#### Users
- `GET /api/users/profile` - Получить профиль пользователя (требует авторизации)
- `PUT /api/users/profile` - Обновить профиль пользователя (требует авторизации)

#### Posts
- `GET /api/posts` - Получить все посты
- `POST /api/posts` - Создать новый пост (требует авторизации)
- `POST /api/posts/:id/like` - Лайк/дизлайк поста (требует авторизации)
- `POST /api/posts/:id/comments` - Добавить комментарий (требует авторизации)

#### Health Check
- `GET /api/health` - Проверка состояния сервера

## 🛠 Технологии

- **Node.js** - Runtime environment
- **Express.js** - Web framework
- **JWT** - Authentication
- **bcryptjs** - Password hashing
- **CORS** - Cross-origin resource sharing
- **Helmet** - Security middleware
- **Morgan** - HTTP request logger

## 🔧 Структура проекта

```
backend/
├── src/
│   ├── controllers/     # Контроллеры (будущее расширение)
│   ├── middleware/      # Middleware функции
│   │   └── auth.js      # JWT авторизация
│   ├── models/          # Модели данных (будущее расширение)
│   ├── routes/          # API роуты
│   │   ├── auth.js      # Авторизация
│   │   ├── users.js     # Пользователи
│   │   ├── posts.js     # Посты
│   │   ├── events.js    # События
│   │   ├── help.js      # Помощь
│   │   └── knowledge.js # База знаний
│   └── server.js        # Главный файл сервера
├── .env.example         # Пример конфигурации
├── .gitignore
├── package.json
└── README.md
```

## 🔒 Безопасность

- JWT токены для авторизации
- Хеширование паролей с bcrypt
- CORS настройки
- Helmet для базовой защиты
- Валидация входящих данных

## 📝 Примеры запросов

### Регистрация
```javascript
POST /api/auth/register
Content-Type: application/json

{
  "name": "Иван",
  "surname": "Петров",
  "email": "ivan@example.com",
  "password": "password123"
}
```

### Авторизация
```javascript
POST /api/auth/login
Content-Type: application/json

{
  "email": "ivan@example.com",
  "password": "password123"
}
```

### Создание поста
```javascript
POST /api/posts
Authorization: Bearer YOUR_JWT_TOKEN
Content-Type: application/json

{
  "title": "Заголовок поста",
  "content": "Содержимое поста",
  "category": "Frontend"
}
```

## 🚧 TODO

- [ ] Подключение базы данных (MongoDB/PostgreSQL)
- [ ] Реализация endpoints для событий
- [ ] Реализация endpoints для помощи
- [ ] Реализация endpoints для базы знаний
- [ ] Файловые загрузки (аватары, изображения)
- [ ] Email уведомления
- [ ] Система ролей и прав доступа
- [ ] Rate limiting
- [ ] API документация (Swagger)
- [ ] Тестирование (Jest)

## 🔄 Интеграция с Frontend

Убедитесь, что в вашем React приложении настроен правильный базовый URL для API запросов:

```javascript
const API_BASE_URL = 'http://localhost:5000/api';
```